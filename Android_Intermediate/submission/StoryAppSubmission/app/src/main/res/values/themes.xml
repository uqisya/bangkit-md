<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.StoryAppSubmission" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.StoryAppSubmission" parent="Base.Theme.StoryAppSubmission" />

    <style name="TitleGeneral" parent="Theme.StoryAppSubmission">
        <item name="android:textColor">@color/background_dark_blue</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textSize">20sp</item>
    </style>

    <style name="ButtonActive" parent="Widget.MaterialComponents.Button">
        <item name="android:backgroundTint">@color/background_dark_blue</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textStyle">bold</item>
        <item name="cornerRadius">25dp</item>
    </style>
</resources>